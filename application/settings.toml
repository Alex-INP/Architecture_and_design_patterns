DEBUG = "True"

TEMPLATES_DIR = "templates"
GLOBAL_TEMPLATES_DIR = "global_templates"

LOGGING = "True"
LOGGER_STDOUT = "console"
LOG_FILE = "mylog"
LOG_FILE_DIR = "logging"

DEFAULT_LOGGER = "True"
DEFAULT_LOGGER_EXCLUDE = []

SITE_ADR = "127.0.0.1"
SITE_PORT = 8000

MIDDLEWARE = [
    "wsgi_framework.middleware.print_environ",
    "wsgi_framework.middleware.favicon_suppress",
    "wsgi_framework.middleware.default_authorization",
    "middleware.custom_middleware.my_custom_middleware"
]

ALLOW_CORS